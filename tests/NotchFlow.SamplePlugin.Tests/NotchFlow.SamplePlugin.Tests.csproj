<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Tests du greffon d'exemple.

    Ils appartiennent au greffon, pas à l'hôte : c'est la façon dont un auteur
    tiers vérifierait son propre code, en ne dépendant que de NotchFlow.Core.

    Seule exception, assumée : la référence à NotchFlow.Infrastructure sert à
    exécuter le chargeur de greffons réel sur l'assemblage compilé. C'est le seul
    moyen de prouver la chaîne complète — assemblage découvert, type instancié,
    fonctionnalité produite — et non seulement que la classe compile.
  -->
  <PropertyGroup>
    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="coverlet.collector" />
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit" />
    <PackageReference Include="xunit.runner.visualstudio" />
  </ItemGroup>

  <ItemGroup>
    <!-- Référencé pour être compilé et copié dans la sortie des tests : le test de
         chargement déplace l'assemblage dans un dossier de greffons temporaire. -->
    <ProjectReference Include="..\..\samples\NotchFlow.SamplePlugin.Weather\NotchFlow.SamplePlugin.Weather.csproj" />
    <ProjectReference Include="..\..\src\NotchFlow.Core\NotchFlow.Core.csproj" />
    <ProjectReference Include="..\..\src\NotchFlow.Infrastructure\NotchFlow.Infrastructure.csproj" />
  </ItemGroup>

</Project>
